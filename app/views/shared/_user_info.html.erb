<%= link_to gravatar_for(current_user, size: Settings.avatar_index_size), current_user %>
<h1><%= current_user.name %></h1>
<span><%= link_to t("users.view_profile"), current_user %></span>
<span><%= t "users.micropost", count: current_user.microposts.size,
  micropost: pluralize(:count, "micropost") %></span>
